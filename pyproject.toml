[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["backend/tests"]
markers = [
    "live: tests that hit the live LLM (skipped unless RUN_LIVE=1)",
]
